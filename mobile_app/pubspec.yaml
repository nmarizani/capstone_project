name: mobile_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
      flutter:
        sdk: flutter
      flutter_bloc: ^9.1.1
      equatable: ^2.0.5
      dio: ^5.8.0+1
      flutter_secure_storage: ^9.2.2
      go_router: ^14.2.7
      connectivity_plus: ^6.0.5
      uuid: ^4.5.1
      intl: ^0.19.0
      camera: ^0.11.0+2 # for eye capture later
      permission_handler: ^11.3.1

cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
